- 景森明确要求的长期规则再次确认并持续生效：回答要简洁；一个问题尽量做完再回答，避免行动一步回一步；优先在原模板/原文件/已定入口上原位升级，不轻易新增平行模板、文件、系统。
- 2026-04-24：围绕 ZeroOne Forge AI交易OS 开始正式建设第一阶段，范围锁定为“信息流层 + 候选池层”，先不做自动下单；已在王林分区建立统一入口目录 `agents/wanglin/ZeroOne_Forge_AI_Trading_OS/`，后续该 OS 相关规则、工作流、schema、数据、pools 默认归入此目录，便于识别、恢复与持续迭代。
- 第一阶段已落地的核心骨架包括：`AI_TRADING_OS_STAGE1_PLAN.md`、`MARKET_OS_STAGE1_WORKFLOW.md`、`market_signal.schema.json`、`pool_item.schema.json`，以及 `watch_pool.json` / `candidate_pool.json` / `execution_pool.json` 三个标准池文件。
- 当前 ZeroOne Forge AI交易OS 的主目标已被景森明确校准：不是局部做“账号池”，而是围绕 Binance + X 两条主线，把信息流层做扎实、做完整、做可信。账号名单/核心账号池只是信息流层的一个子模块，不能替代整体建设目标。
- 信息流层主轴已固定：Binance 为第一主轴，X 为第二主轴，其他来源为补充验证层。后续优先顺序必须遵守：先做稳定获取，再做结构化标准化，再接入 watch/candidate/execution pools。
- Binance 信息流层当前目标范围已明确，应同时覆盖：热门榜、Square、公告、上线、新合约、社区高频讨论；X 信息流层当前目标范围已明确，应同时覆盖：指定账号、指定主题、热门叙事、提及增速、关键币种扩散。
- 2026-04-24 已在 OS 目录内落地并开始使用的 Binance Square 子模块包括：账号抓取标准、账号抓取工作流、Top50 观察池、Core15 核心抓取池、Core15 最近有效帖子输入层与对应刷新工作流；其中用户指定账号 `拉哪(lanaai)` 与 `unclecow` 已纳入 Core15。
- 2026-04-24 过程中，曾出现推进偏差：过度聚焦“账号池/Core15”，把局部模块误当成信息流层主任务。该偏差已被景森指出并纠正。后续推进必须持续对照“AI交易OS最小落地架构图”和 Binance + X 全局信息流目标，避免再次陷入局部最优。
